<!DOCTYPE html>
<html>
<head>
  <title>Admin Panel | YouTube Promotion</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
    rel="stylesheet">
</head>

<body class="bg-light">

<nav class="navbar navbar-dark bg-dark">
  <div class="container">
    <span class="navbar-brand">Admin Panel</span>
    <a href="/dashboard" class="btn btn-secondary btn-sm">Back</a>
  </div>
</nav>

<div class="container mt-4">

  <h4 class="mb-3">All Videos Overview</h4>

  <% if (videos.length === 0) { %>
    <p>No videos uploaded yet.</p>
  <% } %>

  <% videos.forEach(video => { %>
    <div class="card shadow mb-3">
      <div class="card-body">

        <h5 class="mb-1"><%= video.title %></h5>

        <p class="mb-1">
          <strong>Uploaded By:</strong>
          <%= video.userEmail %>
        </p>

        <p class="mb-1">
          <strong>Uploaded On:</strong>
          <%= new Date(video.createdAt).toLocaleString() %>
        </p>

        <p class="mb-1">
          <strong>Total Views:</strong>
          <%= video.views %>
        </p>

        <% if (video.isAdminVideo) { %>
          <span class="badge bg-primary">
            Admin Video
          </span>
        <% } %>

        <% if (video.promoted) { %>
          <span class="badge bg-success ms-2">
            Promoted
            (<%= video.promoViews %> / <%= video.promoTarget %>)
          </span>
        <% } else { %>
          <span class="badge bg-secondary ms-2">
            Normal
          </span>
        <% } %>

      </div>
    </div>
  <% }) %>

</div>

<footer class="mt-5 text-center text-muted">
  © 2026 YouTube Promotion Platform — Admin View
</footer>

</body>
</html>
